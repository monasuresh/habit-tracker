<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Habit Tracker - Add Groups</title>
    <link rel="stylesheet" href="/public/css/group.styles.css">
</head>
<body>
    <header>
        <h1>Habit Tracker - Add Groups</h1>
    </header>
    <div class="button-div">
        <a href="/protected" class="submit-button">Home Page</a>
        <a href="/habits/create-habit" class="submit-button">Manage Habits</a>
        <a href="/tracked-habits/view-habit-log" class="submit-button">Log a Habit</a>
        <!-- Commented out link: <a href="/groups" class="submit-button">Manage Group</a> -->
        <a href="/individual" class="submit-button">Individual Challenge</a>
        <a href="/challenges" class="submit-button">Challenge Dashboard</a>
        <a href="/leadershipboard" class="submit-button">Leadership board</a>
    </div>
    <main>
        <form action="/groups" id="groups-form" method="POST">
            <div class="form-group">
                <label for="groupNameInput">Group Name:</label>
                <input type="text" id="groupNameInput" name="groupNameInput" placeholder="Enter Your Group Name">
            </div>
            <div class="form-group">
                <label for="habitInput">Select Habit:</label>
                <select id="habitInput" name="habitInput">
                    <!-- Habit options will be inserted here -->
                </select>
            </div>
            <div class="form-group">
                <label for="startdateInput">Start Date:</label>
                <input type="date" id="startdateInput" name="startdateInput">
            </div>
            <div class="form-group">
                <label for="enddateInput">End Date:</label>
                <input type="date" id="enddateInput" name="enddateInput">
            </div>
            <div class="form-group">
                <label for="userInput">Select Participant:</label>
                <select id="userInput" name="userInput">
                    <!-- Participant options will be inserted here -->
                </select>
            </div>
            <div class="form-group">
                <button type="submit" class="submitBtn" name="submitBtn">Add Group</button>
            </div>
            <div id="error">
                <p>{{error}}</p>
            </div>
        </form>
    </main>
</body>
</html>
